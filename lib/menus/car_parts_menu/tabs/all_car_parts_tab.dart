import 'package:flutter/material.dart';

class AllCarPartsTab extends StatelessWidget {
  const AllCarPartsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
