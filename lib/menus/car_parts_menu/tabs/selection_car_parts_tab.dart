import 'package:flutter/material.dart';

class SelectionCarPartsTab extends StatelessWidget {
  const SelectionCarPartsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
